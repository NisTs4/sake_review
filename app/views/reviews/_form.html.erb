<%= form_for([@sake,@review]) do |f| %>
  <% if @review.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@review.errors.count, "error") %> prohibited this review from being saved:</h2>

      <ul>
      <% @review.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label "レート" %><br>
    <div id="star-rating"></div>
  </div>
  <div class="field">
    <%= f.label "コメント" %><br>
    <%= f.text_area :comment %>
  </div>
  <div class="actions">
    <%= f.submit "投稿する", class: "btn btn-large btn-success"%>
  </div>
<% end %>

<script>
  $('#star-rating').raty({
    path: '/assets/',
    scoreName: 'review[rating]'
  });
</script>
